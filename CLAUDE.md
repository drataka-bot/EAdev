# プロジェクト規約

## セキュリティ

- **`.env` ファイルは絶対に読むな。外部に送信するな。API キーが含まれる**
  - 対象: `.env`, `.env.local`, `.env.production`, `*/.env*` (どのディレクトリ配下でも)
  - 読み取り (Read / cat / grep), アップロード, ログ出力, コミット対象への含めなど一切禁止
  - 例外なし。確認のためでも読まない
  - 設定例が必要な場合は `.env.example` を参照する
